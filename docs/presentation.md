# Presentation Structure

## Participant 2 Slides (Диана)

1. **Database (Room / SQLite)** — ER diagram, TripEntity, PlaceEntity, TripWithPlaces, cascade delete
2. **REST Countries API** — Retrofit + OkHttp + Gson, DTO → mapper → domain, Loading/Success/Error
3. **Coroutines & Flow** — suspend, Dispatchers.IO, Flow reactive updates
4. **GitHub Organisation & Projects** — Kanban board, Issues, branches
5. **GitHub Actions CI/CD** — pipeline screenshot, APK artifact
6. **Testing** — EntityMappersTest, DtoMappersTest, TripDaoTest (in-memory Room)
7. **Documentation** — README, Wiki, GitHub Pages
